[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "moldea_async"
version = "0.0.2"
description = "The official SDK for accessing and integrating moldea’s API in your applications. "
readme = "README.md"
license = {text = "MIT"}
requires-python = ">=3.12.3"
authors = [
  { name = "Jesus Graterol", email = "jesusgraterol.dev@protonmail.com" }
]
dependencies = [
  "httpx>=0.28.1",
]

[project.urls]
Homepage = "https://github.com/moldea-ai/moldea-async-py"